var React = require('react');

module.exports = React.createClass({

	render: function() {
    return (
    	<div style={{
          position: "absolute",
          top: 50,
          left: -225,
          width: 160,
          height: 40,
          background: "#ffffff",
          zIndex: 200
        }}>

     	</div>
  )}
});
